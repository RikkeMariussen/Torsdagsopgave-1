void setup() {
  methodOne(); 
  methodTwo(); 
}

/*
  The following method has an error in it. Fix the error and run it. 
*/

void methodOne()
{
  int i = 1000; // You are not allowed to change this line. 
  
  int max = 10;
  
  if (i > max)
  {
    String output = (i+ " is greater than "+max+".");   
  println(output);
  } 
}

/* 
  Finish the following method so that we can change the number assigned 
  to the weekday and it prints the correct output.  
*/

/* Pga. sygdom har jeg ikke kunne være til undervisning og er desværre en smule bagud med lektier. På trods af googling
har jeg ikke kunne forstå eventuelt kodesvar. Der var en hvor der stod noget med case og switch. Dog ville jeg ikke skrive kode jeg ikke kunne forstå, så jeg har ikke udfyldt noget, men tager til review i morgen og forhåbentlig hjælper det. */

void methodTwo() 
{
  int weekDay = 1; // 1 = Monday, 7 = Sunday. 
  boolean weekend = false;
  
  if (weekDay <= 5)
  {
    weekend = false;
  }
  else 
  {
    weekend = true;
  }
  
  // Print the name of the weekday here: 
  
    
    
  // Print if it is weekend here:
  
  
}
